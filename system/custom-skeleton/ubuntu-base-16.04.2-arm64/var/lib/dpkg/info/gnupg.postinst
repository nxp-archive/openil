#!/bin/sh
set -e
# Automatically added by dh_installudev
if [ "$1" = configure ]; then
	if [ -e "/etc/udev/rules.d/z60_gnupg.rules" ]; then
		echo "Preserving user changes to /etc/udev/rules.d/60-gnupg.rules ..."
		if [ -e "/etc/udev/rules.d/60-gnupg.rules" ]; then
			mv -f "/etc/udev/rules.d/60-gnupg.rules" "/etc/udev/rules.d/60-gnupg.rules.dpkg-new"
		fi
		mv -f "/etc/udev/rules.d/z60_gnupg.rules" "/etc/udev/rules.d/60-gnupg.rules"
	fi
fi
# End automatically added section
