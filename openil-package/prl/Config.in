config BR2_PACKAGE_PRL
	bool "prl"
	help
	  PRL (Pipe Rate Limiter) is a C program for pipeline manipulation
	  that filters stdin per line, and rate limits stdout to a given rate,
	  dropping excess lines.

